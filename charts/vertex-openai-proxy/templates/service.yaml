apiVersion: v1
kind: Service
metadata:
  name: {{ include "vertex-openai-proxy.fullname" . }}
  labels:
    {{- include "vertex-openai-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "vertex-openai-proxy.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
