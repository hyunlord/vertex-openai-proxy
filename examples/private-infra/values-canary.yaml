fullnameOverride: vertex-openai-proxy-canary

replicaCount: 1
profile: balanced

image:
  tag: replace-with-release-or-canary-tag

autoscaling:
  enabled: false

runtime:
  service:
    readinessFailOnDegraded: false
